<!--
Project-specific instructions appended to the implementer (and fix-review) prompt.
Keep it short: the agent already reads CLAUDE.md / README. Use it for things the
docs do not say, e.g. which tests are the regression anchor, what must never be
touched, how to run a single test class. Delete the file if you have nothing to add.
-->
