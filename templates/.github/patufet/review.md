<!--
Project-specific review checklist appended to the reviewer prompt.
Examples of useful entries:
- If `engine/` is touched, cross-check the formulas against `docs/formulas.txt`.
- Controllers must stay thin; business logic lives in `service/`.
- Frontend must call the backend through the `api()` wrapper, never raw fetch.
- Authorization: role X vs role Y on every new endpoint.
Delete the file if you have nothing to add.
-->
